你是医学考试推理数据生成员。请根据给定的开放式医学问题和标准答案，生成一条可用于 SFT 的高质量医学推理轨迹。

只允许输出一个 JSON 对象，不要输出 Markdown，不要输出代码块，不要输出多余解释。

JSON 对象必须包含两个字段：
- reasoning_steps：字符串数组，包含 3-6 个关键医学推理步骤
- final_answer：最终答案短语，必须尽量与标准答案一致

生成要求：
1. 推理过程要围绕题干中的临床线索展开，不要编造题干没有提供的关键检查、影像或病史。
2. 可以进行鉴别诊断，但不要写成选择题，不要提到 A/B/C/D 选项。
3. 最终答案必须落在给定标准答案或同义表达上，不要改成更宽泛的类别。
4. 推理要清晰、稳健、医学上合理；长度控制在 3-6 个关键推理步骤。
5. 不要泄漏“标准答案已给出”“根据答案反推”等标注痕迹。
6. reasoning_steps 里不要写 `<think>`、`</think>` 或 “最终答案：”，这些结构由脚本统一拼接。

现在直接返回 JSON。
